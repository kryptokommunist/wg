<li data-theme="<%= "b" if !duty.accomplished_at %>"><a href="#" id="<%= duty.id %>" onclick="deleteDuty(this.id)"><%= "#{duty.created_at.strftime("%d.%m.%y, %H:%M")}: #{duty.mate.first_name}, #{duty.area.name}" %><span style="float: right;">Löschen</span></a></li>